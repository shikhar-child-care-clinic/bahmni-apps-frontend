/**
 * Application-level constants for the home app.
 *
 * These were previously sourced from distro/src/constants/app.ts.
 * Kept here so the home module is self-contained and does not depend
 * on the distro shell at runtime.
 */
export const LOGIN_PATH = '/bahmni/home/index.html#/login';
export const CHANGE_PASSWORD_PATH = '/bahmni/home/index.html#/changePassword';
