/**
 * Public API for @bahmni/home-app
 *
 * Exports the IndexPage component used by the distro shell to render
 * the home/dashboard screen at the root route.
 */
export { IndexPage } from './IndexPage';
export { LocationContext, useLocation, LocationProvider } from './context';
export type { UserLocation } from './context';
